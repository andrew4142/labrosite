{% extends '::base.html.twig' %}

{% block body %}

<style>
	.article_wrapper{
		width: 100%;
		height: 100%;
	}
	.article_title{
		
	}
	.article_description{
		
	}
</style>
<h1>Articles</h1>
	<div class="article_wrapper">
		{% for article in articles %}
			<a href="{{ path('labrosite_article_viewpage', {'id':article.id}) }}" class="article_title">{{ article.title }}</a>
			<div class="article_description"><p>{{ article.description }}</p></div>
		{% endfor %}
	</div>
 {% endblock %}
