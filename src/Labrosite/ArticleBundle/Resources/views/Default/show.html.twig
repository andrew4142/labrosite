{% extends '::base.html.twig' %}

{% block body %}


<h1>{{ article.title }}</h1>
	<div class="article_wrapper">
		<div><i>{{ article.description }}</i></div>
		<div class="article_description">{{ article.content }}</div>
		
	</div>
 {% endblock %}
